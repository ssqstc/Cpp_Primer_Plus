//friend class clasp;
//
//class muff;
//class cuff {
//public:
//    void snip(muff &) {}
//};
//class muff {
//    friend void cuff::snip(muff&);
//};
//
//class muff;
//class cuff {
//public:
//    void snip(muff&) {}
//};
//class muff {
//    friend void cuff::snip(muff&);
//};

//不行,要求互斥,必须前向声明类成员定义

//访问不到嵌套类的私有成员变量

//throw不是将控制权返回给调用程序,而是导致程序沿函数序列后退,直到找到包含try块的函数.随后执行匹配的catch语句块.

//按逆序排列catch语句块,由子类到基类

//ps可能为Superb和Magnificent两个对象的指针
//ps只能是Superb对象的指针

//dynamic_cast只允许沿类层次结构向上转换
//static_cast允许向上和向下转换,且允许枚举、整形、数值类型之间转换.

