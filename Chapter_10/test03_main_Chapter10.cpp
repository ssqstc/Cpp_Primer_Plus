#include "test03_head_Chapter10.h"

/*
 * 知识点总结：
 * - 构造函数重载：提供多个构造函数，允许不同方式初始化对象。
 * - 字符数组与字符串处理：使用 `char` 数组存储姓名，并用 `strcpy` 和 `strcmp` 进行字符串操作。
 * - 结构体成员：使用结构体 `golf` 存储完整信息。
 * - 成员函数：提供函数修改和显示对象的成员变量。
 *
 * 注意点：
 * - 字符数组边界：注意字符数组的长度，确保不会越界。
 * - 输入处理：处理输入时，避免缓冲区溢出，并确保读取正确类型的数据。
 */

/*
 * 主函数：演示 Golf 类的使用
 * - 创建 Golf 对象并显示其信息
 */
int main(){
    Golf p1("Jerry", 100); // 使用带参数的构造函数创建对象
    Golf p2; // 使用默认构造函数创建对象

    cout << endl;
    cout << "Now show two Person: " << endl;
    p1.showgolf(); // 显示对象 p1 的信息
    p2.showgolf(); // 显示对象 p2 的信息

    return 0;
}
